@model IEnumerable<AmsApp.Models.Abstract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            AbstractTitle
        </th>
        <th>
            AbstractBody
        </th>
        <th>
            IsReviewedInd
        </th>
        <th>
            EventId
        </th>
        <th>
            SubmitterFirstName
        </th>
        <th>
            SubmitterLastName
        </th>
        <th>
            SubmitterEmail
        </th>
        <th>
            SubmissionTypeId
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AbstractTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AbstractBody)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsReviewedInd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EventId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubmitterFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubmitterLastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubmitterEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubmissionTypeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AbstractId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AbstractId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AbstractId })
        </td>
    </tr>
}

</table>
