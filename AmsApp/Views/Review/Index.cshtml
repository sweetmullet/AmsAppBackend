@model IEnumerable<AmsApp.Models.Reviews>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            UserId
        </th>
        <th>
            AbstractId
        </th>
        <th>
            SubmittedInd
        </th>
        <th>
            ReviewText
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AbstractId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubmittedInd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReviewText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReviewsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReviewsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReviewsId })
        </td>
    </tr>
}

</table>
