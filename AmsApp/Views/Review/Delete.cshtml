@model AmsApp.Models.Reviews

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Reviews</legend>

    <div class="display-label">UserId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.UserId)
    </div>

    <div class="display-label">AbstractId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.AbstractId)
    </div>

    <div class="display-label">SubmittedInd</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.SubmittedInd)
    </div>

    <div class="display-label">ReviewText</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ReviewText)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
