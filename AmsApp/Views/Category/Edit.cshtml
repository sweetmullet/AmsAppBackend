@model AmsApp.Models.Category

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Category</legend>

        @Html.HiddenFor(model => model.CategoryId)

        <div class="editor-label">
            @Html.LabelFor(model => model.CategoryTitle)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CategoryTitle)
            @Html.ValidationMessageFor(model => model.CategoryTitle)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CategoryDesc)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CategoryDesc)
            @Html.ValidationMessageFor(model => model.CategoryDesc)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CategoryActiveInd)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CategoryActiveInd)
            @Html.ValidationMessageFor(model => model.CategoryActiveInd)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
