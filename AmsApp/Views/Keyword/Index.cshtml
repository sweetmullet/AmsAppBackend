@model IEnumerable<AmsApp.Models.Keyword>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            AbstractId
        </th>
        <th>
            KeywordString
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AbstractId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KeywordString)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KeywordId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KeywordId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KeywordId })
        </td>
    </tr>
}

</table>
