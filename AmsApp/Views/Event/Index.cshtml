@model IEnumerable<AmsApp.Models.Events>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            EventCity
        </th>
        <th>
            EventTitle
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EventCity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EventTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EventsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EventsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EventsId })
        </td>
    </tr>
}

</table>
