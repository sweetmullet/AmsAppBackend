@model IEnumerable<AmsApp.Models.SubmissionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            SubmissionTypeDescription
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubmissionTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubmissionTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubmissionTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubmissionTypeId })
        </td>
    </tr>
}

</table>
