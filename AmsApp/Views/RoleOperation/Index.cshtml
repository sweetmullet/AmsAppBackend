@model IEnumerable<AmsApp.Models.RoleOperation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            RoleId
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleOperationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleOperationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleOperationId })
        </td>
    </tr>
}

</table>
